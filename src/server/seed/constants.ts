/** The shared demo account. Kept in a leaf module (no imports) so importing
 * these constants doesn't pull the whole seed/domain graph into a bundle. */
export const DEMO_EMAIL = "demo@regimen.test";
export const DEMO_PASSWORD = "regimen-demo";
export const DEMO_DISPLAY_NAME = "Demo";
